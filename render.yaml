services:
  - type: web
    name: toolhub-api
    env: node
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
